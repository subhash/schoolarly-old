<div class="portlet portletF" id="portlet_featuredUsers">
    <h2><span><%= link_to @group.name, group_path(@group) %> > <%= I18n.t("groups.site.#{@type}.remove_select") %></span></h2>
    <div class="portlet_content">
    	<% form_tag remove_member_group_path(@group) do |f| %>
        <ul>
            <% @profiles.each do |profile| %>
            <li>
            	<%= check_box_tag 'members[]', profile.id, false %>
                <%=link_to icon_for_profile(profile, 'medium'), profile_path(profile), :title => profile.full_name%>
				<%=link_to profile.full_name, profile %>                
            </li>
            <%end %>
			<li>
				<%= submit_tag I18n.t("groups.site.remove.#{@type}"), :class => "button" %>
			</li>
        </ul>		
		<% end %>
    </div>
</div>
