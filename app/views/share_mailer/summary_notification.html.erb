<% @shares.each do |share| %>
-----
<p>
<%= I18n.t("shares.mailer.new.#{share.shareable_type}.detail", :shareable => name_or_title(share.shareable), :shared_to => name_or_title(share.shared_to), :user_name => share.user.name) %>
</p>

-----
<% end %>